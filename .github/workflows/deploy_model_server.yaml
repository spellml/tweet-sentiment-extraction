name: deploy-model-server
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo $SPELL_AUTH
        with:
          SPELL_AUTH: ${{ secrets.SPELL_AUTH }}