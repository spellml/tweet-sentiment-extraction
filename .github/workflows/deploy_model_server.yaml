name: deploy-model-server
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - env:
          SPELL_AUTH: ${{ secrets.SPELL_AUTH }}
        shell: bash
        run: echo "$SPELL_AUTH"